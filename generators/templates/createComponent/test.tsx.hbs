import { render } from '@testing-library/react';

import {{pascalCase component}} from '../{{pascalCase component}}';

describe('{{pascalCase component}}', () => {
  test('rendered component', () => {
    const { container } = render(<{{pascalCase component}} />);
    expect(container).toHaveLength(1);
  });
})
