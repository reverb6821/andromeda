import { Canvas, Meta } from "@storybook/blocks";
import * as TooltipStory from './Tooltip.stories.tsx';

<Meta of={ TooltipStory } title="Tooltip" />

# Tooltip

Description for Tooltip component.

<Canvas of={ TooltipStory.Default } />

## Usage

```ts
import Tooltip from '@/components/Tooltip/Tooltip;

const Example = () => {
  return (
      <Tooltip/>
  );
};

export default Tooltip;
```

#### Arguments

- **prop title** - prop description
- **prop title** - prop description
- **prop title** - prop description
- **prop title** - prop description
