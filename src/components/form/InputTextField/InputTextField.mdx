import { Canvas, Meta } from "@storybook/blocks";
import InputTextField from "./InputTextField.tsx";
import * as InputTextFieldStory from "./InputTextField.stories.tsx";

<Meta of={InputTextFieldStory} title="Input Text Field" />

# Input Text Field

Input Text Field component with different props.

<Canvas of={InputTextFieldStory.InputTextFieldExample} />

## Usage

```ts
import InputTextField from '@/components/form/InputTextField/InputTextField';

const InputTextField = () => {
  return (
      <InputTextField
        model='login.email'
        label="E-Mail"
        icon: 'ri-mail-send-line'
        />
  );
};

export default InputTextField;
```
#### Arguments
- **label** - A string that represents the text content of the field.
- **disabled** - A boolean indicating whether the button should be disabled or not. When disabled, the button cannot be clicked or interacted with.
- **icon** - A string indicating whether the button should have a icon. It accept remix icon font class
- **helper** - A string that represents the description of the content field.
- **placeholder** - A string that represents the text content of the field.
- **model** - A string that represents the field name inside formik form.
- **value** - Represents the field value.


