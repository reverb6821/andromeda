import {{pascalCase route}} from '../__test__{{pascalCase route}}';

describe('{{pascalCase route}}', () => {
  test('renders learn react link', () => {
    const { getByText } = render(<{{pascalCase route}} />);
    expect(getByText('{{pascalCase route}} Component')).toBeInTheDocument();
  });
})
