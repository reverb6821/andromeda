import { Canvas, Meta } from "@storybook/blocks";
import Footer from "../Footer.tsx";
import * as FooterStory from "./Footer.stories.tsx"

<Meta of={FooterStory} title="Footer" />

# Footer

Footer component with different props.

#### Footer

<Canvas of={FooterStory.FooterExample} />

## Usage

```ts
import Footer from '@/components/ui/navigation/Footer';

const Footer = () => {
  return (
        <Footer footerTitle='Andromeda' footerEl={footerEl}/>
  );
};

export default Header;
```