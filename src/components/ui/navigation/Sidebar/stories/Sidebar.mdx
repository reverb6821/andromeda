import { Canvas, Meta } from "@storybook/blocks";
import Sidebar from "../Sidebar.tsx";
import * as SidebarStory from "./Sidebar.stories.tsx"

<Meta of={SidebarStory} title="Sidebar" />

# Sidebar

Sidebar component with different props.

#### Sidebar

<Canvas of={SidebarStory.SidebarExample} />

## Usage

```ts
import Sidebar from '@/components/ui/navigation/Sidebar';
import Logo from '@/assets/logo-placeholder-image.png'
import Grid from '@/components/ui/Grid/Grid';
import Typography from './components/ui/Typography/Typography';


  const SIDENAV_ITEMS = [
    {
      title: 'Home',
      path: '/',
      icon: 'ri-dashboard-line',

    },
    {
      title: 'Projects',
      path: '/projects',
      icon: 'ri-bar-chart-2-line',
      submenu: true,
      subMenuItems: [
        { title: 'All', path: '/projects' },
        { title: 'Web Design', path: '/projects/web-design' },
        { title: 'Graphic Design', path: '/projects/graphic-design' },
      ],
    },
    {
      title: 'Messages',
      path: '/messages',
      icon: 'ri-discuss-line',
    },
    {
      title: 'Settings',
      path: '/settings',
      icon: 'ri-equalizer-line',
      submenu: true,
      subMenuItems: [
        { title: 'Account', path: '/settings/account' },
        { title: 'Privacy', path: '/settings/privacy' },
      ],
    },
    {
      title: 'Help',
      path: '/help',
      icon: 'ri-question-line',
    },
  ];

const Example = () => {
  return (
        <Sidebar
          items={SIDENAV_ITEMS}
          sidebarLogo={Logo}
          sidebarTitle='Andromeda'
          sidebarSubTitle='Component Collection'
        >
          <Grid direction='column' classNames='items-center justify-center dark:bg-slate-600'>
            <Typography textSize='caption' textStyle='secondary' text={t('placeholder')} />
          </Grid>
        </Sidebar>
  );
};

export default Example;
```