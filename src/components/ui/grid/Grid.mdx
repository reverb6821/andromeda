import { Canvas, Meta } from "@storybook/blocks";
import Grid from "./Grid.tsx";
import * as GridStory from "./Grid.stories.tsx";

<Meta of={GridStory} title="Grid" />

# Grid

Grid component with different props.

<Canvas of={GridStory.GridExample} />

## Usage

```ts
import Badge from '@/components/partials/Badge';

const Badge = () => {
  return (
    <Grid direction='column' classNames='m-auto items-center justify-center dark:bg-slate-600'>
        <div className='bg-emerald-600 py-4 px-2 w-64 max-md:w-full'/>
        <div className='bg-sky-600 py-4 px-2 w-64 max-md:w-full'/>
        <div className='bg-yellow-600 py-4 px-2 w-64 max-md:w-full'/>
        <div className='bg-red-600 py-4 px-2 w-64 max-md:w-full'/>
    </Grid>
  );
};

export default Button;
```

#### Arguments
