export interface HeaderProps {
    headerTitle: string,
    headerSubTitle: string,
    headerEl: itemProps[],
    headerLogo: string
}

export interface itemProps {
    label: string,
    route: string,
}